manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
  projects:
    - name: zmk
      remote: zmkfirmware
      revision: v0.3-branch
      import: app/west.yml
    # Явно указываем полную ссылку на графический модуль YADS
    - name: zmk-dongle-screen
      url: https://github.com
      revision: main
      path: zmk-dongle-screen
  self:
    path: config
